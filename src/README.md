# BinMark Source Notes

The app is implemented with Qt Quick Controls and the local ImageManager C++ object.

Build helper targets:

- Script-UpdateTranslations updates TS/QM translation files.
- Script-DeployRelease runs the Qt deployment helper for release builds on Windows.
